# Council brief — template (v1.0.0)

    council:     <short-lane-name>
    facilitator: <role>
    convened:    <UTC ISO-8601>
    bounded question: "<EXACTLY ONE question, in quotes>"
    decision record (output): <lane-dir>/decision-record.md
    terminal line: COUNCIL COMPLETE <path> | BLOCKED <reason>

## Context (state as of <date>)

- <verifiable fact> (source: <live read / artifact path>)
- <contested or stale item> - labeled STALE/CONTESTED, not ground truth

## Out of scope (do not re-litigate)

- <closed decision or closed blocker, with the reference that closed it>

## Required output form

1. <e.g. separate immediate (24-48h) / near-term (1-2 wk) / exploratory>
2. <e.g. highest-leverage sequence and dependencies>
3. <e.g. team-autonomous vs operator-decision split>
4. <e.g. risks, stale assumptions, contradictions in the current queue>
5. No more than N concrete actions; each with owner lane, acceptance signal, stop condition.
6. <at least one option outside the current queue, so the council does not tunnel>

## Boundaries

No external posts, no merges, no promotion, no canonical edits. Deliberation only.

## Model receipt

<role → configuration model per seat, read at dispatch>
